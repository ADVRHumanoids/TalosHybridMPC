[build-system]
requires = ["flit_core >=2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "taloshybridmpc"
version = "0.1.0"
description = "Talos-specific IBRIDO MPC package."
authors = [{name = "AndrePatri"}]
readme = "README.md"
license = {file = "LICENSE.md"}
classifiers = ["License :: OSI Approved :: MIT License"]
